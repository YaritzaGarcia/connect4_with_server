name: 'Folder Watcher'
inputs:
  folder-name:
    description: 'The name of the folder that we want to watch.'
    required: true
    default: 'templates'
runs:
  using: "composite"
  steps:
    - name:
      run: "echo 'There was a change in this folder: <b>$INPUT_FOLDER</b>.'"
      shell: bash
      env:
        INPUT_FOLDER: ${{ inputs.folder-name }}